let arr: number[] = [1,2,3,4]; // simple array decleration.
let arr2: (string | number)[] = [1,2,3,4,4,'3']; // mix of both values in the array.
let arr3: string[] | number[] = []; // Either the array can be of numbers or the strings.

let seatAlocation: "window" | "middle" = "window" // yoy can define the value that is going to be in the variable. Other then   that no value is going to be in the variable.





